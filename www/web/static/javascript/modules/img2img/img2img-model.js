import { Model } from '../core/model.js';

export class Img2imgModel extends Model {
    constructor(params) {
        super(params);
        this.url = `${aid.api.host}/nodes`
    }

// # Textual inversion embeddings file path: fp


}
